export default function Main({children}: {children: React.ReactNode})
{
    return (
        <main
        id="MAIN"
        className="super_scroll_bar o_h_a"
        >
            {children}
        </main>
    )
}